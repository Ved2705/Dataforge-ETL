name: dataforge
version: '1.0.0'
config-version: 2
profile: dataforge
model-paths: ["models"]
test-paths: ["tests"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  dataforge:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
